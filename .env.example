# Azure AD Configuration

# ===================================================================
# DEFAULT CONFIGURATION (Built-in Multi-Tenant App)
# ===================================================================
# The server includes a pre-registered multi-tenant Azure AD app:
# - Client ID: f44ab954-9e38-4330-aa49-e93d73ab0ea6
# - Tenant ID: common (multi-tenant support)
#
# You can use this app with DeviceCode authentication without any
# environment variables - just set AUTH_METHOD=DeviceCode
# ===================================================================

# Optional: Azure AD Tenant ID
# - Leave unset to use 'common' (multi-tenant, default)
# - Set to your specific tenant ID for single-tenant scenarios
# AZURE_TENANT_ID=your-tenant-id-here

# Optional: Azure AD Client (Application) ID
# - Leave unset to use the built-in multi-tenant app
# - Set to your own app registration if needed
# AZURE_CLIENT_ID=your-client-id-here

# Azure AD Client Secret (required for ClientSecret auth method only)
# - Get this from Azure Portal > App Registrations > Certificates & secrets
# - Not needed for DeviceCode or ManagedIdentity methods
AZURE_CLIENT_SECRET=your-client-secret-here

# Optional: Logging level (DEBUG, INFO, WARN, ERROR)
LOG_LEVEL=INFO

# Optional: Authentication method
# - InteractiveBrowser: Opens browser for login (RECOMMENDED, no secrets needed, easiest)
# - DeviceCode: Interactive auth via device code (no secrets needed)
# - ClientSecret: Use client credentials flow (requires AZURE_CLIENT_SECRET)
# - ManagedIdentity: Use Azure Managed Identity (for Azure-hosted scenarios)
AUTH_METHOD=InteractiveBrowser
